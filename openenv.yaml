name: navis-web-env
slug: navis_web_env
version: 0.1.0
description: Deterministic mock website navigation environment for evaluating web agents with OpenEnv.
entrypoints:
  app: server.app:app
  client: client:NavisWebEnv
models:
  action: models:NavisWebAction
  observation: models:NavisWebObservation
  state: models:NavisWebState
docker:
  dockerfile: server/Dockerfile
tags:
  - openenv
  - web-navigation
  - deterministic
  - reinforcement-learning
