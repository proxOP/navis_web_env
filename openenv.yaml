name: navis-web-env
slug: navis_web_env
version: 0.1.0
description: Deterministic mock website navigation environment for evaluating web agents with OpenEnv.
entrypoints:
  app: tasks.server.app:app
  client: tasks.client:NavisWebEnv
models:
  action: tasks.models:NavisWebAction
  observation: tasks.models:NavisWebObservation
  state: tasks.models:NavisWebState
docker:
  dockerfile: Dockerfile
tags:
  - openenv
  - web-navigation
  - deterministic
  - reinforcement-learning
